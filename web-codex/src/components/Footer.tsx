import React from 'react';

const Footer: React.FC = () => {
  return (
    <footer>
      <p>&copy; {new Date().getFullYear()} Solo Leveling Codex. All rights reserved (conceptually).</p>
    </footer>
  );
};

export default Footer;
