import React from 'react';

const Header: React.FC = () => {
  return (
    <header>
      <h1>Solo Leveling Codex</h1>
      <nav>
        {/* Placeholder for navigation links if needed later */}
        {/* Example: <a href="/">Home</a> */}
      </nav>
    </header>
  );
};

export default Header;
